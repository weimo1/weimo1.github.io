---
title: 操作系统
date: 2026-06-20
categories:
  - ["系统底层408", "操作系统"]
publish: true
---

# 操作系统

## 什么是操作系统

* 运行在计算机上的软件程序，管理计算机硬件与软件资源的程序
* 屏蔽了硬件层的复杂性
* 操作系统的内核（Kernel）是操作系统的核心部分，它负责管理了
* 内存管理
* 文件系统管理
* 硬件设备管理
* 应用程序管理

## 什么是系统调用

先了解下用户态和系统态：

* **用户态**：用户态运行的进程可直接读取用户程序的数据
* **系统态**：系统态运行的进程或程序可以访问**计算机的任何资源**

我们运行的程序基本都在用户态，当我们需要调用系统态的功能时，此时就需要系统调用，系统调用按功能可分：

* 设备管理：设备的请求、释放、启动等
* 文件管理：文件的读写、创建、删除等
* 内存管理：进程的创建、撤销、阻塞、唤醒等
* 进程控制：进程间的消息、信号传递
* 进程通信：内存分配、回收以及获取作业占用内存大小及地址

## 用户态、内核态

**用户态是指应用程序运行在非特权模式下的状态，具有较低权限，不能直接操作硬件资源和底层系统资源，只能访问被授权的资源。**

**内核态是指操作系统运行在特权模式下的状态，具有最高权限，可以访问所有硬件资源和底层系统资源，如cpu、内存、IO。**

当应用程序执行一些特殊权限的操作，比如IO，就需要发起系统调用进入内核态。

用户态切换到内核态有三种方式：**系统调用、外部中断、异常**

* 系统调用是**用户态主动要求切换到内核态**，主要是为了使用内核态的特殊权限操作如文件管理、进程管理、内存管理。系统调用的过程是，用户态发起系统调用后，会通过软中断指令切换到内核态，然后执行中断处理程序，也就是执行系统调用请求，处理完后再次触发中断切换回用户态。（软中断指令：使用int指令触发中断向量0x80，执行中断处理程序）
* 外部中断是**外围设备完成用户请求的操作后**，会向cpu发出中断信号，cpu此时如果执行的是用户态的程序，就会切换到内核态，然后去**执行中断处理程序**。例如磁盘读写完成，cpu会切换到磁盘读写的中断处理程序去执行后续操作。
* 异常是**用户态发生了异常**，需要切换到内核态**来执行异常处理程序**。

当发生用户态到内核态的切换时，会发生如下过程（本质上是从“用户程序”切换到“内核程序”）

* 设置处理器至内核态。
* 保存当前寄存器（栈指针、程序计数器、通用寄存器）。
* 将栈指针设置指向内核栈地址。
* 将程序计数器设置为一个事先约定的地址上，该地址上存放的是**系统调用处理程序**的起始地址。

而之后从内核态返回用户态时，又会进行类似的工作。
